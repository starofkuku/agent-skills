interface:
  display_name: "Release Tag Push"
  short_description: "Commit, tag, and push a new release"
  default_prompt: "Use $release-tag-push to commit the current changes, create the next project version tag, and push the branch and tag."
